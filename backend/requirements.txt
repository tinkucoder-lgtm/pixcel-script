fastapi==0.136.3
anyio==4.13.0
uvicorn[standard]==0.24.0
python-multipart==0.0.6
pillow==10.1.0
pydantic==2.13.4
python-dotenv==1.0.0
aiofiles==23.2.1
requests==2.34.0
google-cloud-vision==3.14.0
pytesseract==0.3.13
# RapidOCR — PP-OCRv4 detection + recognition models served via ONNX runtime.
# No paddlepaddle dependency (works on Apple Silicon arm64 + Python 3.12).
# Models are bundled in the wheel (~80MB). First request after server restart
# pays ~1-3s to load models into memory; subsequent requests reuse cache.
rapidocr-onnxruntime==1.3.24
uharfbuzz==0.54.1
freetype-py==2.5.1
